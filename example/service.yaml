# port is not read by the service — set the listening port via uvicorn CLI or Docker -p flag
spec_path: /openapi.json
info:
  title: My Merged API
  version: 1.0.0
# auth:
#   username: admin
#   password: secret
